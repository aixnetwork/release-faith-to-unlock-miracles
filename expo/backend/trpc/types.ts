// Type-only export file for client-side usage
// This file is designed to be safe for client-side imports
// to prevent @trpc/server runtime code from leaking into the bundle

export type { AppRouter } from './app-router';
